<div class="page-wrap">
  <h1>{{ article.title }}</h1>
  <div style="color:var(--ink-dim);font-size:13px;margin-bottom:24px">{{ article.published_at | date: '%Y.%m.%d' }}</div>
  <div style="line-height:1.9;color:var(--ink)">{{ article.content }}</div>
</div>
